We analyze US gun murder data collected by the FBI

download~data.R download csv file to data directory

wrangle~data.R ~ creates a derived dataset and saves an R object in RDA directory

analysis.R generates a plot and saves it to the figs directory